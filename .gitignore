# =============================================================================
# arbv2 — .gitignore
# Anything secret, machine-specific, or build-generated stays OUT of the repo.
# =============================================================================

# -----------------------------------------------------------------------------
# Secrets — NEVER commit
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*_keystore
secrets/
private/

# -----------------------------------------------------------------------------
# Runtime data — contains live wallet balances, RPC URLs (with API keys), etc.
# -----------------------------------------------------------------------------
logs/
*.log
status/
*.pid
# Discovery store — runtime-generated, changes as the bot learns new pools/tokens
discovery/

# -----------------------------------------------------------------------------
# Rust build artifacts
# -----------------------------------------------------------------------------
target/
**/*.rs.bk
*.pdb

# -----------------------------------------------------------------------------
# Foundry build artifacts + libraries
#   - lib/        third-party submodules (forge-std, openzeppelin) — fetched via `forge install`
#   - cache/      forge build cache, leaks RPC URL in deploy logs
#   - out/        compiled ABIs/bytecode (regenerated by `forge build`)
#   - broadcast/  deploy tx history, leaks RPC URL + signer addr
# -----------------------------------------------------------------------------
contracts/lib/
contracts/cache/
contracts/out/
contracts/broadcast/

# -----------------------------------------------------------------------------
# Python virtualenvs / caches (dashboard.py is pure stdlib but in case)
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# -----------------------------------------------------------------------------
# Editor / IDE
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.cursor/
.cursorignore

# -----------------------------------------------------------------------------
# OS junk
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# Local scratch / experiment files (free space to play in without risk of leak)
# -----------------------------------------------------------------------------
scratch/
local/
TODO.local.md
NOTES.local.md
